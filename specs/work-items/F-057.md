# F-057 - Conversa, controle de acesso e ingestão concorrente

**Status:** done

## Source and outcome

- Source: `specs/001-mvp-document-intelligence.md`, sections 4, 6, 7 and 10; user request of 2026-09-23.
- Outcome: compact conversation layout, authenticated API surface, admin-only integration management, and bounded concurrent ingestion where safe.
- Invariants: organization and folder isolation; no OAuth secrets or document content in logs; idempotent jobs with explicit terminal states.

## Decisions and scope

| Decision | Owner | Status | Rationale |
| --- | --- | --- | --- |
| Keep health and authentication entry/callback routes public; require identity elsewhere. | Existing spec | Approved | Login must be possible before a session exists. |
| Owner and Admin can manage integrations; Member cannot. | Existing spec | Approved | Owner has all organization capabilities and Admin connects sources. |
| Bound concurrency at external I/O and independent CPU/embedding work; keep ORM sessions thread-confined. | Implementation | Approved | Avoid provider throttling and transaction races. |
| Preserve the existing local worktree changes. | User context | Approved | The working tree already contains unrelated in-progress changes. |

## Ready checklist

- [x] Acceptance criteria and scope derived from user request and approved spec.
- [x] Module owners and files assigned; parallel edits confined to disjoint areas.
- [x] Protected decisions are covered by existing spec.
- [x] Error, authorization, isolation, and performance risks identified.

## Ownership

| Role | Files | Deliverable |
| --- | --- | --- |
| UI implementation | `frontend/app/*` | Conversation layout and admin navigation. |
| Auth implementation | `backend/app/api/*`, `backend/app/main.py`, auth API tests | Authentication and role enforcement. |
| Ingestion implementation | `backend/app/ingestion/*`, `backend/app/integrations/*`, ingestion tests | Bounded concurrency. |
| Coordinator | This dossier and spec | Integration and gate. |
| Independent validator | Read-only final review | Acceptance and regression report. |

## Acceptance criteria

1. The conversation view can hide and reopen the right file panel with an accessible button; add source appears in the left column and the top bar is removed.
2. Unauthenticated protected requests are rejected; public login/health flow remains functional.
3. Member cannot access integration management APIs or the Tools UI; Owner and Admin can.
4. External document fetch and safe processing/embedding work use bounded concurrency with stable result handling and unchanged tenant scope.
5. Failures remain visible and jobs reach correct terminal states; relevant positive, negative and isolation tests pass.

## Evidence and gate

| Command/check | Result |
| --- | --- |
| Focused auth, integration and ingestion tests, independent rerun | 72 passed |
| Full backend `pytest -q` | 236 passed, 6 skipped, 3 failed in preexisting multiscope/provenance work outside F-057 |
| `ruff check app tests` | Passed |
| Frontend `tsc --noEmit` | Passed |
| Frontend `npm run lint -- --quiet` | Passed |
| `git diff --check` | Passed |
| `.tools/graphify/bin/graphify update .` | Passed; graph refreshed |

Independent validator: no blocking F-057 finding. It confirmed accessible file-panel toggle without a top bar, source action in the left library column, session requirements on private routers, Owner/Admin integration management, and bounded provider/embedding concurrency with ORM work on the job thread. The validator found a Notion reconnect provider-confusion defect during review; this was fixed in both OAuth stages and covered by a regression test. The full-suite failures are two multiscope citation-selection expectations and one source URL prompt expectation in earlier dirty work; they remain to be resolved separately. A timing benchmark is not yet available, so no latency reduction is claimed. Gate decision: F-057 accepted with those unrelated baseline test limitations recorded.

The prescribed `scripts/init-work-item.sh` is absent from this checkout; this dossier was initialized manually.
