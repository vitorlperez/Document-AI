# F-038 — Aplicativo alinhado ao exemplo da landing

**Status:** done

## Source and decisions

User requests aligning the app design and name with the LP example. Reference: `frontend/app/landing-page.tsx` ProductPreview and Brand, `landing.css`; spec 001 §§6, 10, 15. Approved presentation: lowercase `arquivio.` wordmark with Layers3 mark; paper/sage/forest palette, quiet borders, sources/conversation/library desktop composition and readable mobile layout. Product name in prose remains Arquivio.

No backend, contracts, authorization, provider, billing, retention or deployment changes. Preserve all existing actions and scope/citation/error semantics. No fake demo content in production screens.

## Ready checklist

- [x] Scope and presentation reference explicit.
- [x] Frontend owns changes; no API/database change.
- [x] Existing organization/folder boundaries and requests preserved.
- [x] Responsive, keyboard and failure-state checks required.

## Ownership

- Design implementation agent: frontend/app/product-app.tsx, globals.css, landing-page.tsx, landing.css and new shared brand/style files only.
- Root: favicon, dossier, specification note, isolated temporary synthetic preview server and browser QA.
- Independent validator: read-only final diff review and lint/build checks, no implementation.

## Acceptance criteria

- Given LP/login/app, branding matches `arquivio.` and shared icon treatment.
- Given desktop workspace, sources, conversation and library follow the LP's paper/sage/forest design while all existing actions remain available.
- Given 390px screen, no horizontal overflow; context, composer, navigation and sources remain accessible.
- Given questions/documents/errors, real scoped requests, citations and recovery behaviors are preserved.
- Given team/integrations/onboarding, consistent product identity and readable focus/error states remain.

## Validation

- `npm run lint` and `npm run build` in frontend: passed, including fresh checks on 2026-09-22.
- Independent validator reviewed shared Brand, favicon, semantic colors, visible focus, scoped requests and existing handlers; approved code after root fixed stale-document display during loading/error in the right library panel. Final lint passed independently.
- Synthetic browser: source → folder → document list; selected context → question → cited answer → expanded excerpt; save question and next draft preserved conversation. Negative question produced insufficient-evidence text.
- Desktop viewport 1280px: no horizontal overflow, three panels (225px / 751px / 270px). At 390×844: scrollWidth 390px; context before composer; viewport reset after test.
- Product uses actual API items; synthetic fixtures exist only in `/tmp/arquivio_f038_preview.py`, not production sources. No real provider certification.
- Shared LP/login/workspace wordmark, light auth surfaces and role labels inspected. Team/integration handlers reviewed; no fresh integration OAuth flow is claimed.
- Graphify AST updated 2026-09-22: 2451 nodes, 6261 edges. Semantic documentation status is not updated by AST-only command.

## Gate

Approved for F-038 presentation scope: independent code/lint gate plus root build/browser QA. No unresolved F-038 blocker. Validator subsequently hit account limit after sending its approval; approval evidence is its delivered message, not a later final report.

## Concurrent editing boundary (2026-09-22)

User informed root that Copilot is implementing tools concurrently. Since that notice, root made no further frontend/backend/compose edits; only read-only checks, generated build/graph outputs and this dossier. Subsequent tools/auth/support changes are outside this gate. Design agent also hit its limit; root completed its unfinished CSS and UI work before the concurrency notice.
