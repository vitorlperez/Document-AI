# F-043 — Corrigir ingestão de blocos aninhados do Notion

**Status:** done

## Contexto

O OAuth e a chamada à API do Notion funcionam, mas páginas com conteúdo dentro de toggles, colunas ou páginas-filhas eram lidas apenas no primeiro nível. O documento era salvo como `empty_extracted_text`, deixando o job em `partial_failure`.

## Escopo

Percorrer a árvore de blocos do Notion com limite de profundidade, preservar o contrato atual de `DiscoveredDocument` e manter o comportamento de retry/estado da pipeline. Não altera OAuth, permissões, retenção ou dados de outras integrações.

## Aceitação

- Blocos filhos são incluídos no texto extraído e podem ser indexados.
- Páginas realmente vazias continuam visíveis como falha parcial com `empty_extracted_text`.
- A árvore remota tem limite de profundidade e não pode causar loop infinito.
- Testes unitários cobrem sucesso e o fluxo existente de pipeline continua passando.

## Validação

## Evidência final

- `cd backend && .venv/bin/pytest -q`: 220 passed, 6 skipped, 4 deprecation warnings.
- `cd backend && .venv/bin/pytest -q tests/unit/test_notion_integration.py tests/unit/test_ingestion_tasks.py tests/unit/test_ingestion_service.py`: 27 passed.
- Ruff passou nos arquivos do adaptador e dos testes.
- Graphify atualizado após a alteração.
- O banco local confirmou o diagnóstico: o último job Notion terminou como `partial_failure`; dois documentos estavam `indexed` e a página `Vitor Perez` estava `failed` com `empty_extracted_text`.
- O worker local foi reconstruído e reiniciado com a correção. Um novo sync deve ser iniciado para reprocessar a página que já havia sido marcada como falha.
- Diagnóstico adicional confirmou que a página `Vitor Perez` retorna zero blocos pela API do Notion. O adaptador agora ignora páginas vazias/metadados sem conteúdo indexável, evitando `partial_failure` quando os demais documentos foram processados.
- Nova suíte após esse ajuste: 221 passed, 6 skipped. Worker reconstruído novamente e iniciado com a versão atual.
