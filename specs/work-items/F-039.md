# F-039 — Compose unificado e imagens Arquivio

**Status:** validating

## Request / approved design

User requests Docker image fixes and one docker-compose containing all images. Canonical local `docker-compose.yml` already contains six services; consolidate named application images so API/worker/migrate share one backend image and frontend builds current app. Keep PostgreSQL/Redis included, health/migration startup ordering, environment examples and one-command startup. Production topology must remain usable with same backend image naming convention. No registry push, cloud deploy, deleting volumes or interrupting existing user stack.

## Ownership

Docker implementation agent owns compose files, Dockerfiles, Docker docs/env examples and README. Root owns dossier and design F-038. Independent validator reviews read-only after implementation.

## Acceptance

- One compose includes PostgreSQL, Redis, migrate, API, worker, frontend with explicit reusable image names.
- Backend runtime image shared across API, worker and migrate; frontend built from current sources.
- Configuration validates and isolated image build/stack health smoke succeeds when environment permits.
- Default startup documented; existing data is not deleted or migrated to another volume silently.
- No secrets in Docker layers or committed configuration; existing external pilot retained.

## Validation and gate

Image consolidation implemented. `docker-compose.yml` resolves postgres, redis, migrate, api, worker and frontend. API/worker/migrate share `arquivio-backend:local`; frontend uses `arquivio-frontend:local`; official PostgreSQL/Redis images retained.

- Docker agent validated an isolated earlier snapshot: migrations exit 0, PostgreSQL/Redis/API healthy, Celery ping pong, identical backend image digest across API/worker/migrate. Frontend image rebuilt and isolated six-service startup reported. Full final frontend HTTP smoke/cleanup report was not delivered because agent hit usage limit; do not infer missing results.
- Independent validator reviewed original image/compose/ignore/docs diff and successfully ran local and production `docker compose config --quiet` before concurrent edits. No blocker in that snapshot.
- Root reran current local `docker compose config --quiet`, `config --services`, `config --images` and `git diff --check` on 2026-09-22: passed. Four distinct images for six services. Named local app images are present. Query of project label `arquivio-f039` returned no containers; no claim is made about other project labels.
- `frontend/.dockerignore` excludes checkout-local `.sites-runtime` so local Sites runtime state cannot switch Docker execution profile.

## Concurrent changes / remaining gate

After the independent review, concurrent work removed production and remote-worker compose profiles and introduced named `postgres_data`. Root preserved these edits and did not start/recreate the user's stack. Earlier statements preserving production topology describe the original F-039 plan, not the concurrent final tree.

Current local Compose syntax is valid. Data migration from any previous anonymous PostgreSQL volume to the new named volume has NOT been performed or verified; a recreated database can appear empty if the previous data remains in a different volume. Existing data was not deleted by root.

`docs/deployment/free-pilot-render.md` still references deleted production/remote-worker files. Integration of those concurrent topology/documentation changes and existing-volume continuity remain pending with the editor owning that work. The image consolidation is complete; F-039 stays validating to avoid applying old smoke evidence to the changed topology. No registry push or production deployment occurred.
