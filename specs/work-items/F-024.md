# F-024 - Área de ferramentas e gestão da conexão Google Drive

**Status:** done

## Source and outcome

- Source specification: `specs/001-mvp-document-intelligence.md` F2, section 7 (security/privacy) and F-017 reauthorization behavior.
- User outcome: the current integrations route becomes a generic **Tools** catalog. Selecting Google Drive opens a management modal that identifies the authorized Google account when available, exposes its connection state, supports reauthorization, and safely disconnects it so another account can later be authorized.
- Non-goals: a second external connector, changing Drive OAuth scope, deleting indexed Company knowledge on disconnect, revoking access from Google remotely, or changing workspace/RAG access rules.

## Decisions

| Decision | Owner | Status | ADR / rationale |
| --- | --- | --- | --- |
| Use Drive `about.get` with the existing `drive.readonly` grant to capture only the authorized account e-mail after OAuth completion | Product + implementation | approved | The Google method accepts the current scope; the e-mail makes the account action understandable without expanding consent |
| Persist the authorized account e-mail as connection metadata and return it only to Owner/Admin within its organization | Product + implementation | approved | Required for the requested connection modal; no token or document content is exposed |
| Disconnect clears locally retained encrypted credentials and account metadata, marks the source `disconnected`, and retains existing indexed snapshots | Product + implementation | approved | Existing specification requires only that future syncs stop; removing knowledge is a separate destructive action |
| Reauthorization reuses the source UUID and may authorize a different Google account | Product + implementation | approved | Keeps workspace/source links stable while requiring the new account to authorize access before further syncs |

## Ready checklist

- [x] Acceptance criteria written in Given/When/Then form.
- [x] Owning module, API/data impact and failure states identified.
- [x] Authorization, tenant and workspace-folder impact assessed.
- [x] Performance and observability impact assessed.
- [x] Protected decisions approved or marked not applicable.

## Ownership

| Role | Module/files owned | Deliverable |
| --- | --- | --- |
| Specification analyst | Read-only | Ready checklist and test matrix |
| Implementation owner | `backend/app/integrations/*`, `backend/app/api/integrations.py`, migration, `frontend/app/product-app.tsx` | Source lifecycle API and Tools modal |
| Test engineer | `backend/tests/api/test_google_integrations.py`, frontend build | Authorization and lifecycle coverage |
| Feature validator | Read-only | Independent gate decision |

## Acceptance criteria and test matrix

| Criterion | Test layer | Evidence |
| --- | --- | --- |
| Given an Owner or Admin opens Tools, when they select Google Drive, then a modal shows its available/disconnected/reauth-required state and the authorized account e-mail when known. | API + frontend build | Passed: source contract returns safe account metadata; Tools card/modal compiles and opens automatically after OAuth. |
| Given an Owner or Admin selects Reconnect, when OAuth completes, then the same source UUID is connected and its authorized Google account metadata is refreshed without exposing tokens. | API | Passed: OAuth reauthorization regression asserts one source UUID, refreshed e-mail and no raw token response. |
| Given an Owner or Admin confirms Disconnect, when it completes, then stored credentials and account metadata are cleared, source status is `disconnected`, and subsequent source operations cannot use it. | API | Passed: disconnect regression asserts nullable credential/email, status and rejected scope catalog. |
| Given a Member or a user from another organization attempts source listing, reauthorization, or disconnect, when the request is made, then it is denied and the target source remains unchanged. | API | Passed: member/cross-tenant lifecycle checks return 403 and preserve the source. |
| Given a disconnected source has historical indexed data, when disconnect completes, then no workspace/documents are deleted as a side effect. | API | Passed: workspace folder, document and Library node counts remain unchanged. |

## Commands and results

| Command | Result | Run by | Independent rerun |
| --- | --- | --- | --- |
| `.venv/bin/ruff check app tests alembic/versions/20260914_0013_source_account_metadata.py` | Passed | Implementation | Passed |
| `.venv/bin/pytest -q tests/api/test_google_integrations.py tests/unit/test_google_drive_connection.py tests/unit/test_ingestion_tasks.py` | 29 passed | Implementation | Independently reviewed |
| `.venv/bin/pytest -q` | 160 passed, 6 skipped (2 upstream deprecation warnings) | Implementation | Passed by validator before deployment check |
| `npm run build` | Passed | Implementation | Passed by validator |
| `docker compose exec -T api alembic current` | `20260914_0013 (head)` | Implementation | Independently rerun by validator |
| `curl --fail --silent --show-error http://localhost:8000/health/live` | `{\"status\":\"ok\"}` | Implementation | Independently rerun by validator |

## Validator report

- Blocking: none. Initial local deployment was on the previous migration and was rebuilt/applied before approval.
- Important: none.
- Suggestions: show a dedicated multi-account list only if product deliberately supports multiple Google connections per Company.
- Independent evidence: validator reran code/build checks, verified tenant/role and source lifecycle behavior, confirmed `20260914_0013 (head)`, healthy API and recreated frontend/worker.
- Gate decision: APPROVE.
