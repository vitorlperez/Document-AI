# F-058 - Componentes consistentes em Conversa e Biblioteca

**Status:** done

## Source and scope

- Source: user request of 2026-09-23 and `specs/001-mvp-document-intelligence.md`, entry flow.
- Reuse the same Biblioteca sidebar header and breadcrumb controls in the Conversation and Library screens.
- Put Add source in the left column on both screens, subject to Owner/Admin access.
- Let the right sidebar on each screen be collapsed and reopened without a full-width top header.
- Preserve each page's distinct content and actions.

## Acceptance and ownership

- Both screens render the same source heading, Add source and Refresh controls, and breadcrumb styles.
- The right panel on each screen has an accessible toggle and can reopen.
- Member never sees Add source.
- Frontend TypeScript and lint checks pass; independent validator reviews final diff.
- Implementation owner: `frontend/app/product-app.tsx`, `frontend/app/chat-workspace.css`.
- Validator: read-only independent pass.

## Gate

- Independent review confirmed shared sidebar controls, Owner/Admin visibility, accessible right-panel toggles and preserved local navigation state.
- `npx tsc --noEmit`, `npm run lint`, and `git diff --check` passed. Lint has five existing warnings and no errors.
- Validator found a minor border mismatch at 1024–1279 px; desktop panel borders were aligned with the grid breakpoint.
- No blocking findings. F-058 accepted.
