# F-056 - Deduplicação de fontes citadas

**Status:** validating

## Source and outcome

- Source specification: `specs/001-mvp-document-intelligence.md`, section F4.
- User outcome: each original document appears once in “Fontes citadas”, and the same excerpt does not repeat within that document's citation card.
- Non-goals: changing retrieval/ranking, model citation selection, API payloads, authorization, tenant/folder scope, or citation excerpt length.

## Decisions

| Decision | Owner | Status | Rationale |
| --- | --- | --- | --- |
| Group copies by normalized original source URL, falling back to the local document ID when no URL exists. | Implementation owner | Approved | Local IDs can differ for copies of the same source file; URL is already part of each citation. |
| Consolidate excerpts that match after Unicode normalization and whitespace collapsing; preserve all distinct page numbers on the single displayed excerpt. | Implementation owner | Approved | Removes repeated citation text without losing where the excerpt was found. |
| Keep distinct excerpts for the same document and keep documents with different source URLs separate. | User intent | Approved | The request is to remove duplicates, not remove relevant evidence or merge separate sources. |

## Ready checklist

- [x] Acceptance criteria written in Given/When/Then form and checked against F4.
- [x] Owning module, API/data impact, and failure behavior identified.
- [x] Authorization and tenant impact assessed; this is presentation-only over already authorized citations.
- [x] Performance impact assessed; linear pass over at most the existing citation list.
- [x] Protected decisions are not changed; no confirmation is required.

## Ownership

| Role | Module/files owned | Deliverable |
| --- | --- | --- |
| Implementation owner | `frontend/app/product-app.tsx`, `specs/001-mvp-document-intelligence.md` | Consolidated source cards and unique passages with page metadata. |
| Feature validator | Read-only | Review final diff, duplicate behavior, source separation, and tenant-scope preservation. |

## Acceptance criteria and evidence plan

| Criterion | Validation layer | Evidence |
| --- | --- | --- |
| Given citations from multiple local copies of the same source URL, when rendered, then one source card is shown. | UI review | `compactCitations` groups by normalized source URL and falls back to the local document ID. Independent review passed. |
| Given the same excerpt occurs more than once in one document, when rendered, then the excerpt appears once and all known page numbers are retained. | UI review | Excerpts are normalized for Unicode/whitespace, collapsed, and their page numbers are merged into one citation passage. Independent review passed. |
| Given a document has different excerpts or two documents have different source URLs, when rendered, then distinct evidence and documents remain visible. | UI review | Only equal normalized excerpts within the same source key merge. Independent review passed. |
| Given a response contains only one citation per document, when rendered, then the existing card, excerpt, and original-link behavior remains intact. | UI review | The card still renders the excerpt and original link; a single page uses the existing page label. Independent review passed. |

## Commands and results

| Command | Result | Run by | Independent rerun |
| --- | --- | --- | --- |
| Automated tests | Not added or run for this request. | — | — |
| `.tools/graphify/bin/graphify update .` | Passed; refreshed 2,741 nodes, 6,987 edges, and 198 communities. | Implementation owner | Not required |

The prescribed `scripts/init-work-item.sh` is absent from this checkout, so this dossier was initialized manually before implementation. Automated test changes are deferred under the session instruction not to add or run tests unless requested.

## Validator report

- Blocking: none.
- Important: none.
- Suggestions: none.
- Independent evidence: validator confirmed normalized source grouping, fallback to local IDs, unique excerpt consolidation with all known pages, separation of different sources and excerpts, and preservation of original links. No retrieval, API payload, or tenant-scope behavior changed. No tests or checks were run.
- Gate decision: implementation and independent review are complete; automated test validation remains deferred.
