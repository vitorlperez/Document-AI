# F-060 - Links somente nas referências da resposta

**Status:** done

## Source and outcome

- Source: user request of 2026-09-23 and `specs/001-mvp-document-intelligence.md` F4.
- The answer body has no source URLs or Markdown links, even if the model returns them. Links remain on cited document rows only.
- Keep factual answer text and the cited-document payload; no API shape or database change.

## Design and decisions

- Strengthen the existing API response sanitizer, shared by folder and organization questions, after citation validation. The model instruction alone is insufficient.
- Preserve readable Markdown link labels while removing targets; remove bare HTTP/HTTPS, `www`, and provider source URLs. Remove empty source-only lines after cleanup.
- Do not alter document `source_url` in citations or the front-end link to the original.
- No authorization or tenant-scope changes.

## Acceptance and evidence

- Folder and organization question serializers return clean answer text when generation includes Markdown and bare URLs; cited `source_url` remains intact.
- Normal text and insufficient-evidence (`answer: null`) remain unchanged.
- Relevant API/unit tests, Ruff, full backend suite, and independent read-only validation pass.

## Ownership and gate

- Implementation: `backend/app/api/ingestion.py` and focused API tests.
- Independent validator: read-only review and rerun.
- `scripts/init-work-item.sh` is absent; dossier created manually.
- Focused multiescope API tests: 23 passed; full backend suite: 243 passed, 6 skipped.
- Ruff and `git diff --check`: passed; graphify updated.
- Independent validator initially found URL fragments left by nested parentheses and source-only Markdown lines. The sanitizer and tests were revised; independent rerun found no blockers. It verified folder and organization serialization, preserved answer labels and cited `source_url`, and removal of bare, parenthetical, Markdown, and source-only links.
- Gate decision: accepted.
