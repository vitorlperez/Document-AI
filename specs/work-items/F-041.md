# F-041 — Dimensões uniformes nas páginas internas

**Status:** done

## Scope / authorization

User requests replicating chat dimensions across other pages for uniformity. Extend F-040 full-width shell, 16px desktop / 6px mobile page gutters, 10px vertical header padding and compact page titles to library, team, integrations and support. Public landing and centered authentication forms retain their purpose-specific layouts. Source spec 001 §6/§10 and sdd-feature-delivery workflow.

## Ownership

Root: shared shell class attributes, library header class, staff shell class, new product-layout.css and extraction of shell sizing out of chat-workspace.css. No tools/integrations handlers, API, auth or Copilot behavior changes. Validator: read-only independent review/checks.

## Acceptance

- Chat/library/team/integrations share outer width, gutters and global header dimensions.
- Support follows same outer spacing; retains distinct operational styling.
- Page titles/spacing compact, main content benefits from available width, dialogs keep bounded width.
- Desktop/mobile no horizontal overflow; forms/navigation work; no business changes.

## Validation

- `cd frontend && npm run build`: passed. Independent validator ran `npm run lint` and `git diff --check`: passed; existing `<img>` warning in integrations remains outside F-041.
- Independent code review approved selector scope, bounded dialogs, responsive field widths and preserved handlers/API; conditional browser/build evidence fulfilled.
- Synthetic desktop QA at 1280×720: Library, Team and Integrations panels each 1248px wide with 16px side gutters and global header 59px high, matching chat shell.
- At 390×844: all three panels 378px wide with 6px gutters, document scrollWidth 390px. Team form and integrations content remained accessible.
- Google Drive modal opened through the existing catalog button and remained 512px wide on desktop; no external connection action taken.
- Support checked with synthetic staff identity: desktop container 1280px with padding `10px 16px 16px`; mobile container 390px with 6px gutters and no horizontal overflow. Operational appearance preserved.
- Library fixture initially returned a missing-endpoint error for workspace-folders; the visible toast remained usable. This was a synthetic-fixture limitation, not a production API change.
- Graphify AST update passed: 2472 nodes, 6281 edges, 178 communities. Semantic documentation is not refreshed by AST-only updates.
- Preview tabs/viewport reset and temporary servers cleaned up after QA. No real-provider or Copilot feature validation claimed.

## Final gate

Approved: independent validator found no blocker; root build and desktop/mobile geometry checks completed. No edits to tools/integration behavior, backend or Docker in this feature. Public landing and authentication layouts intentionally preserved.
