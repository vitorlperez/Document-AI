# F-061 - Remover referências inline da resposta exibida

**Status:** done

## Source and outcome

- Source: screenshot and user report of 2026-09-23; F4 in `specs/001-mvp-document-intelligence.md`.
- The reported response contains `(google_drive: URL)` and numeric `[1]` markers inside the answer. They must not appear in the conversation; original links remain only in the cited-document list.

## Decisions and ownership

- Preserve server-side answer cleaning and extend it for provider-URL attribution and numeric citation markers.
- Apply the same display cleaning in the frontend so an older or inconsistent API response cannot expose inline links in the conversation. Keep cited `source_url` untouched.
- Implement in `backend/app/api/ingestion.py`, relevant API tests, `frontend/app/answer-display.ts`, `frontend/app/product-app.tsx`, and a small frontend regression test.
- No API shape, tenant scope, retrieval, or database changes.
- Independent validator reviews final diff and reruns checks.

## Acceptance

- Screenshot-shaped answer with provider URL and `[1]`-style markers displays cleanly and keeps factual text.
- Markdown and bare links, source-only lines, and empty punctuation lines are removed from the answer body.
- Cited document URLs remain available in the reference list; insufficient-evidence answer remains null.
- Backend API, frontend sanitizer, TypeScript, lint and full backend tests pass.

`scripts/init-work-item.sh` is absent; dossier created manually.

## Evidence and gate

- Frontend `node --experimental-strip-types --test tests/answer-display.test.mjs`: 2 passed; `tsc --noEmit` and lint: passed, with five existing nonblocking lint warnings.
- Backend multiescope API tests: 23 passed; full backend suite: 243 passed, 6 skipped; Ruff passed.
- `git diff --check` and `.tools/graphify/bin/graphify update .`: passed.
- Independent validator confirmed that screenshot-shaped provider URLs and `[n]` markers disappear from both folder/organization API answers and the frontend display. Factual text remains; citation `source_url` stays intact and `answer: null` still shows the retrieval message.
- No blocking findings. Gate decision: accepted.
