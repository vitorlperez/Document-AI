# F-040 — Mais espaço para a conversa

**Status:** done

## Approved scope

User requests giving the main chat more screen space. Apply sdd-feature-delivery, source spec 001 §6 and §10, F-038 visual identity. Chat uses available window width, compact heading/context/composer; optional sources/library panels start collapsed and can be toggled without losing conversation, draft or selection. Mobile keeps context before composer and no horizontal overflow.

## Ownership and concurrency

Root: ONLY ConversationLibraryWorkspace markup/state, one stylesheet import in product-app.tsx, new chat-workspace.css, this dossier. Do not modify tools/integrations/backend/Copilot changes. Independent validator read-only review/checks after implementation.

## Acceptance

- Default chat uses available width with no side panels taking space.
- Fontes/Biblioteca buttons expose matching panels and accessible expanded state; selection/draft/messages retained on toggle.
- Desktop transcript gains height from compact chrome; mobile no overflow and controls usable.
- No API, permission, provider or business behavior changes.

## Validation and gate

- `cd frontend && npm run lint`: no errors; one concurrent `<img>` warning in integrations, outside F-040. Independently rerun by validator.
- `cd frontend && npm run build`: passed. `git diff --check`: passed.
- Independent validator approved code, four column states, hidden/aria-expanded controls, retained mounted state, API/permission preservation and desktop/mobile CSS; conditional build/browser evidence fulfilled below.
- Synthetic browser at 1280×720: default conversation width 1246px (previous F-038 measurement 751px, +65.9%); transcript 357px high. Sources only: 1036px; library only: 976px; both: 766px. No horizontal overflow in any combination.
- Sent question returned a cited answer. Draft “Meu próximo rascunho”, message and selected context survived opening/closing panels.
- At 390×844, conversation width 376px, document scrollWidth 390px; context precedes composer. Opening Biblioteca kept no horizontal overflow and retained draft. Visual inspection passed. Synthetic API only; no real providers exercised.
- Graphify AST update executed after code changes. Temporary preview and viewport override cleaned up.

Gate: approved for F-040 by independent validator plus root build/browser evidence. No unresolved blocker. Copilot tools/integrations edits preserved; F-040 only touches the conversation component, its stylesheet import and new scoped stylesheet.
