# F-062 - Referências numéricas ligadas à lista de documentos

**Status:** done

## Source and outcome

- Source: user request of 2026-09-23, F4 in `specs/001-mvp-document-intelligence.md`.
- Answer prose identifies supporting documents only as `fonte 1` or `fontes 1 e 2`, matching the numbered document list below. It does not attribute facts with document names or URLs.
- The document list is numbered. After the first three rows, an accessible toggle reveals all remaining rows in normal vertical flow and can collapse them again.

## Decisions

- Model citations are evidence indexes. After validating them, map each cited evidence index to the ordinal of its original source document in the returned citation list. Multiple chunks or local copies of one original source share one number.
- Keep the existing `citations` API shape and source URLs in those citations; display names and links only in the numbered reference list.
- Preserve the existing source-backed answer and tenant/folder isolation rules. Uncited or invalid numeric markers do not become references.
- The frontend still cleans raw links from an older API response but preserves validated `fonte N` prose returned by the current API.

## Acceptance and tests

- `[2]` referring to the second listed document becomes `fonte 2`; adjacent citations to multiple documents become `fontes 1 e 2`; chunks of one document share one ordinal.
- Unknown/uncited indexes never point to a document not in the returned reference list.
- Document rows show stable numbering, name, tool and original link. Expansion reveals every remaining row and the toggle changes to a collapse action.
- Insufficient-evidence responses remain without answer or references; cross-organization isolation remains.
- Focused backend/frontend tests, TS/lint/Ruff, full backend suite, and independent validator review pass.

## Ownership and gate

- Backend: `backend/app/knowledge/questions.py`, `backend/app/api/ingestion.py`, focused API/unit tests.
- Frontend: `frontend/app/product-app.tsx`, `frontend/app/answer-display.ts`, focused frontend tests.
- Coordinator: spec/dossier and integration; independent validator: read-only final review.
- `scripts/init-work-item.sh` is absent; initialized manually.
- Evidence: backend `pytest -q` (246 passed, 6 skipped); frontend answer display tests (3 passed), `tsc --noEmit`, ESLint, and `git diff --check` passed. `graphify update .` completed.
- Independent validator confirmed semantic and inventory marker conversion, list numbering, vertical expansion/collapse, and frontend/backend ordering. Residual model behavior: prompt discourages document names in prose, while deterministic cleanup avoids deleting names that are factual answer content.
